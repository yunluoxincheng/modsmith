# Web Application

Status: planned implementation package.

Responsibilities:

- prompt input and generation job creation
- job progress display
- ModSpec preview and validation feedback
- build log and repair attempt display
- artifact download UI

The web app must treat planned adapters as unavailable unless the server reports them as implemented or production-ready.
